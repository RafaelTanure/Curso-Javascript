
const raio = 5.6;
// temos uma constante PI, e o pow que é uma potencia que é uma função 
const area= Math.PI * Math.pow(raio,2);

// é do tipo object. Não é uma função 
console.log(area);
console.log(typeof Math);
