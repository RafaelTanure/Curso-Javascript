const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d; // Operador de soma
const subtracao = d - b; // Operador de subtração 
const multiplicacao = a * b; // Operador de multiplicação
const divisao = d / a; // operador de divisão 
const modulo = a % 2; // Operador que pega o resto da operação 

console.log(soma, subtracao, multiplicacao, divisao, modulo);
