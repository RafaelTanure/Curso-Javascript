const resultado = nota  => nota >=7 ? 'Aprovado' : 'Reprovado' ; 
//Utilização de Operador ternário para que retorne true ou false verificando se a nota é maior ou menor que 7.

console.log(resultado(7.1)); 
console.log(resultado(6.7)); 
